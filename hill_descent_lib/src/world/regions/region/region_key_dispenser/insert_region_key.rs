use crate::world::regions::region::region_key_dispenser::RegionKeyDispenser;

impl RegionKeyDispenser {
    pub fn insert_region_key(&mut self, full_region_key: Vec<usize>) -> RegionKey {
        // get all adjacent full keys

        // merge zones if necessary

        // assign new zone key if necessary (and increment next zone key)

        // assign new key (and increment next key)

        // insert and return region key

        todo!()
    }
}
