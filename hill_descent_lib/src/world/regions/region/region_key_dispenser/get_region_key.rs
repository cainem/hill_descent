impl RegionKeyDispenser {

    pub fn get_region_key(&mut self, full_key: Vec<usize>) -> RegionKey {

        // searches the key_mapper for an existing key and returns if found

        // if not found then search all existing keys looking for a spatially adjacent full keys

        // if not found then we have a 


        todo!();

    }


}