use crate::{Phenotype, world::regions::region::Region};

impl Region {
    pub fn reproduce(&self, number_to_reproduce: usize) -> impl Iterator<Item = Phenotype> {
        // return if number_to_reproduce == 0

        // order the organisms as per pdd.md

        // select top number_to_reproduce.

        // pair for reproduction as per the pdd.md

        // return new Phenotypes.

        todo!();
    }
}
